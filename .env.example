# RentCast API key (https://developers.rentcast.io) — used only by server/index.mjs.
# The app works fully without this; it falls back to seeded June 2026 data.
RENTCAST_API_KEY=

# Port for the rent proxy server (optional, defaults to 4517)
RENT_PROXY_PORT=4517
